{
  "name": "lecturescribe",
  "version": "1.0.0",
  "private": true,
  "description": "LectureScribe — Turn your lectures into structured notes with dual-engine AI",
  "scripts": {
    "postinstall": "npm --prefix backend install",
    "start": "cd backend && npm start",
    "build": "cd frontend && npm install && npm run build",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "sidecar": "cd backend && npm run sidecar"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
